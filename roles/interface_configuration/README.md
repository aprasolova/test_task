The role ensures that the main network interface on a server is called with specific name. Set the name with `interface_configuration__main_iface_name` variable.

`netplan` is used to apply the configuration.
