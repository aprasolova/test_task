The role takes a disk and creates a partition of given name and number on all its size.
To manage the partition name, use `disks_partitioning__disk_name` and `disks_partitioning__partition_number` variables.
